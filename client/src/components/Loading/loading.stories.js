import React from 'react';
import Loading from './index';
// import { Wrapper } from './style';

export default {
  title: 'Loading',
  component: Loading,
};

export const LoadingComponent = () => {
  return <Loading />;
};

LoadingComponent.storyName = 'Loading';
